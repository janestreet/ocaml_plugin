(embed
 ((names (run))
  (libraries
   (core
    core_kernel
    fieldslib
    sexplib))
  (cmis (plugin_intf))
  (pps (ppx_jane))))

(executables
 ((names (run))
  (libraries
   (async
    bin_prot
    core
    ocaml_plugin
    sexplib))
  (modules (run plugin_intf))))

(enforce_style ())
