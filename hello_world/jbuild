
(executables
 ((names (run))
  (libraries
   (async
    bin_prot
    core
    ocaml_plugin
    sexplib))
  (modules (run plugin_intf))
  (preprocess (pps (ppx_jane)))))


(jbuild_version jane_street)
