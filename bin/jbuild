(executables
 ((names (ocaml_embed_compiler))
  (libraries
   (async
    core
    ocaml_plugin
    sexplib))
  (preprocess (pps (ppx_jane)))))

(install
 ((section bin)
  (files
   ((ocaml_embed_compiler.exe as ocaml-embed-compiler)))))


(provides (ocaml-embed-compiler (file ocaml_embed_compiler.exe)))

(jbuild_version jane_street)
