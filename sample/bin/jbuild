
(executables
 ((names (run))
  (libraries
   (async
    core
    core_kernel
    ocaml_plugin
    ocaml_plugin_sample
    sexplib))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
