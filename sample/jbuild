(embed
 ((names (run))
  (cmis (dsl))))

(executables
 ((names (run))
  (libraries
   (async
    bin_prot
    core
    core_kernel
    ocaml_plugin
    sexplib))))

(enforce_style ())

(jbuild_version jane_street)
